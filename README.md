# Scraper async Platform

an async platform for simple scrapping task with playwright 

other technology which is used:
- SQLAlchemy
- Fast api
- pydantic (in next version)
- async programming

# install

pip install -r requirements.txt
python -m uvicorn api:app

